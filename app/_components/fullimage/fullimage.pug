mixin fullimage(namespace)
  - namespace = (namespace) ? namespace + '-' : ''
  td.inner.contents.fullimage
    block
    if (!block)
      img(src=`http://quickimage.it/${container*1.5}x${container/1.2}`, width=container, alt='', mc:edit=`${namespace}image`)