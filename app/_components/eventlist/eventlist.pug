
mixin eventlist(namespace, options)
  - namespace = (namespace) ? namespace + '-' : ''
  - var settings = Object.assign({title: 'This is an event title', img: "http://placehold.it/285x160", url: "#"}, options)
  - settings.rsvp = settings.rsvp || settings.url

  .event
    <!--[if (gte mso 9)|(IE)]><table width="100%"><tr><td width="#{width / 3}" valign="top"><![endif]-->
    .column.event-image
      table(width='100%')
        tr
          td.inner
            a(href=settings.url, target="_blank")
              img(src=settings.img, width="190", alt="", mc:edit=`${namespace}event-image`)

    <!--[if (gte mso 9)|(IE)]></td><td width="#{width / 2}" valign="top"><![endif]-->
    .column.event-description
      table(width='100%')
        tr
          td.inner.contents
            h5(mc:edit=`${namespace}event-title`)
              a(href=settings.url, target="_blank") #{settings.title}
            .textlink(mc:edit=`${namespace}event-desc`)
              block
              if (!block)
                a(href=settings.url, target="_blank") October 1 | Lincoln Center Campus

    <!--[if (gte mso 9)|(IE)]></td><td width="#{width / 6}" valign="top"><![endif]-->
    .column.event-rsvp
      table(width='100%')
        tr
          td.inner
            +btn(`${namespace}event-btn`, {size: "sm", secondary: true, block: true, url: settings.rsvp}) RSVP
    .event-hr

mixin eventmore(namespace)
  - namespace = (namespace) ? namespace + '-' : '';
  td.inner.contents.event-more(mc:edit=`${namespace}event-more`)
    block
    if (!block)
      a(href="http://www.alumni.fordham.edu/calendar/list.aspx", target="_blank") +20 more events this month. View the calendar.
