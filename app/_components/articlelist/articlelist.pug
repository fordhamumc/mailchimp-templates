mixin article-list--container
  td.inner.contents.article-list--container
    ul.article-list
      block

mixin article-list--item(namespace)
  - namespace = (namespace) ? namespace + '-' : ''

  li(mc:repeatable=`${namespace}article-list`)
    .callout(mc:edit="article-title", mc:hideable) This is a default title.
    .article-list--desc.textlink(mc:edit="article-desc", mc:hideable)
      block
      if (!block)
        | This is some default content. It's not meant to be very long. It's just a preview of the article.

mixin article-list--more(namespace)
  - namespace = (namespace) ? namespace + '-' : ''
  li.article-list--more(mc:edit="article-more", mc:hideable)
    block
    if (!block)
      | View more.
      .break &nbsp;